name: Test tap-zendesk-sell

on:
  push:
    branches: [main]
    paths:
    - .github/workflows/test.yml
    - tap_zendesk_sell/**
    - tests/**
    - pyproject.toml
    - uv.lock
    - tox.ini
  pull_request:
    branches: [main]
    paths:
    - .github/workflows/test.yml
    - tap_zendesk_sell/**
    - tests/**
    - pyproject.toml
    - uv.lock
    - tox.ini
  workflow_dispatch:

env:
  FORCE_COLOR: 1
  TAP_ZENDESK_SELL_ACCESS_TOKEN: ${{ secrets.TAP_ZENDESK_SELL_ACCESS_TOKEN }}

jobs:
  pytest:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version:
        - "3.9"
        - "3.10"
        - "3.11"
        - "3.12"
        - "3.13"
    steps:
    - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@e797f83bcb11b83ae66e0230d6156d7c80228e7c # v6.0.0
      with:
        python-version: ${{ matrix.python-version }}
    - run: pipx install tox
    - name: Run Tox
      run: |
        tox -e $(echo py${{ matrix.python-version }} | tr -d .)
